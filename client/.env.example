# SwipeHire client environment configuration
# Copy to .env. Anything prefixed VITE_ is embedded in the build; never put secrets here.

VITE_API_URL=http://localhost:3001
VITE_SOCKET_URL=http://localhost:3001
